# Dependencies
# This folder contains all the installed npm packages.
# It should be regenerated by running `npm install` on any machine.
/node_modules

# Environment Variables
# This file contains secret credentials (API keys, passwords, etc.).
# NEVER commit this file to version control.
.env

# Session Cache
# This folder is created by whatsapp-web.js to store your session data.
# It is specific to the machine it was created on and should not be shared.
/.wwebjs_auth

# Log files
# Any log files generated during runtime.
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel deployment artifacts
# This folder is sometimes created by the Vercel CLI locally.
.vercel

# Operating System generated files
.DS_Store
Thumbs.db